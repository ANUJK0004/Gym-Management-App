import 'package:flutter/material.dart';

class MemberProgressScreen
    extends StatelessWidget {
  const MemberProgressScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(
        child: Text(
          'Progress',
        ),
      ),
    );
  }
}