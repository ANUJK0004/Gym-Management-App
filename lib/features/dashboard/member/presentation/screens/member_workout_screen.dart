import 'package:flutter/material.dart';

class MemberWorkoutScreen
    extends StatelessWidget {
  const MemberWorkoutScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(
        child: Text(
          'Workout',
        ),
      ),
    );
  }
}